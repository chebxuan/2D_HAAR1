## 项目说明（最新版本）

本项目实现三个量子算术模块，并在此基础上构建了"二维量子形态学小波原型电路"。电路包含量子舍入算子 UR（向下取整），并可对所有 4 位输入进行穷举验证。

### 目录结构

#### 核心电路文件（`circuit/`目录）
- `qquantum_module.py`：提供 `QFT / IQFT / MADD` 指令。
- `qmadd_gate.py` / `qmsub_gate.py` / `c_qmsub_gate.py`：模加、模减、比较-减法器。
- `main_round.py`：最新版主电路（含 UR 算子与 guard bit）。

#### 测试和验证
- `test_rounding.py`：单元测试，验证基准参数的输出。
- `verify_all_inputs.py`：遍历 65,536 组 4 位输入，逐一对比量子输出与经典结果。
- `complexity_analysis.py`：计算电路复杂度（门数、深度、量子比特数）。

#### 图像处理应用
- `edge_detection_experiment.py`：⭐ **核心应用** - 多方向特征分解实验，提取所有小波系数（Cameraman图像）。
- `process_house_image.py`：⭐ **核心应用** - House图像处理实验，展示量子形态学小波在结构化图像上的几何边缘保留能力。
- `binary_morphology.py`：二值图像形态学操作（腐蚀、膨胀）。
- `analyze_results.py`：图像分析工具，统计像素分布和对比结果。

#### 结果可视化
- `generate_core_results_figure.py`：生成核心实验结果的2行3列LaTeX图代码。
- `generate_circuit_figures.py`：生成电路图的LaTeX代码。

### 主电路工作流程（`circuit/main_round.py`）

1. **比较/求差**：使用 `C_QMSUB` 得到 `(a-b)`、`(c-d)` 及比较位。
2. **UR₁ / UR₂**：先计算 `(a-b)+(c-d)` 与 `(a-b)-(c-d)`，再通过 UR 算子向下取整（模 2⁴）。
3. **恢复原值并排序**：对 `a,c` 执行 QMADD 恢复输入，再用 `cswap` 获得 `max/min` 配对。
4. **全局运算**：比较 `min(a,b)` 与 `min(c,d)` 得到 `min(a,b,c,d)`，同时计算 `(a+b)-(c+d)` 并再执行 UR₃。
5. **测量**：读取 `reg_a`（UR₃ 结果）、`reg_d`（四数最小值）、`result1`、`result2`（分别对应 UR₁、UR₂）。

所有寄存器采用 `data_bits + 1` 位（默认 5 位），最高位为 guard-bit，用于记录模运算中的溢出/借位。输出只取回低 `data_bits` 位。

### 使用方法

#### 电路测试和验证
```bash
cd circuit
python main_round.py          # 运行单组参数，打印量子/理论结果
python test_rounding.py       # 运行单元测试
python verify_all_inputs.py   # 穷举所有 4 位输入（需数分钟）
cd ..
python complexity_analysis.py # 计算电路复杂度
```

所有脚本默认都使用 `AerSimulator(method="matrix_product_state")`，可在普通 CPU 上完成仿真。

#### ⭐ 核心应用：House图像边缘检测（paper_outline_ver1.md 核心实验）

House图像是一个典型的结构化图像，包含清晰的几何边缘和规则纹理。此实验作为**核心结果**，展示量子形态学小波在结构化图像上的几何边缘保留能力。

```bash
# 处理house.tiff：转换为二值图像后进行量子形态学小波变换
python3 process_house_image.py --image house.tiff --threshold 128

# 或使用专门的脚本生成 Figure 5 所需图像
python3 generate_figure5_binary_edges.py
```

**输出文件**：
- `house_binary.png` - 二值化图像（阈值=128）
- `house_binary_classical_haar_edges.png` - 经典线性Haar边缘（基于二值图）
- `house_binary_qmhwt_edges.png` - QM-HWT边缘（基于二值图）

**实验重点**（根据 paper_outline_ver1.md）：
- **实验设计**：将原始 House 图像进行二值化处理（阈值=128），分别应用经典线性 Haar 小波和 QM-HWT 进行边缘检测
- **阈值化融合方法**：对三个细节系数（$H_1$, $H_2$, $H_3$）进行硬阈值处理（阈值=30），然后融合并反色处理（白底黑线效果）
- **QM-HWT 优势**：提取的屋顶和墙角边缘清晰锐利，单像素边缘保持完好，几何结构特征明显
- **经典线性 Haar 问题**：在边缘处存在明显的"灰度光晕" (Artifacts) 和模糊，特别是在屋顶边缘区域

#### 🔬 扩展应用：多方向特征分解（Cameraman图像，可选）

针对自然图像（Cameraman），展示**"多方向特征分解能力"**和**"系数稀疏性"**。此实验不在 paper_outline_ver1.md 的核心大纲中，可作为扩展实验。

```bash
# 基本使用（默认处理cameraman.bmp）
python edge_detection_experiment.py

# 指定图像
python edge_detection_experiment.py --image your_image.bmp --bit-depth 4
```

**输出文件**：
- `*_quantum_low_freq.pgm`：低频近似子带（LL, Reg_d）- 展示信息保留和压缩特性
- `*_quantum_horizontal.pgm`：水平细节（H₁）- 横向纹理提取
- `*_quantum_vertical.pgm`：垂直细节（H₂）- 纵向纹理提取
- `*_quantum_diagonal.pgm`：对角细节（H₃）- 角点和斜向边缘提取
- `*_classical_haar_*.pgm`：经典线性Haar对比（3个方向）

**实验重点**：
- **多方向特征分解能力**：展示水平、垂直、对角三个方向的细节提取
- **系数稀疏性**：通过直方图分析展示形态学小波产生更稀疏的表示

#### 二值图像形态学操作

```bash
# 对灰度图像进行二值化后处理
python binary_morphology.py --image cameraman.bmp --bit-depth 4 --binarize --threshold 128

# 或直接处理已经是二值的图像
python binary_morphology.py --image your_binary_image.bmp --bit-depth 4
```

**输出**：
- `*_quantum_erosion.pgm`：量子形态学腐蚀结果
- `*_classical_erosion.pgm`：经典形态学腐蚀对比（3×3窗口）
- `*_classical_dilation.pgm`：经典形态学膨胀对比

#### 生成核心实验结果图

```bash
# 生成2行3列的LaTeX图代码
python generate_core_results_figure.py --image cameraman

# 输出：core_results_figure_latex.tex
```


### 复杂度分析

记数据宽度为 `n`。

| 模块 | 量子比特 | 受控相位门数量 | 深度估计 |
|------|----------|----------------|----------|
| QFT / IQFT | `n` | `n(n-1)/2` | `O(n)` |
| MADD / MSUB | `2n` | `n(n+1)/2`（每个 `cp` 与参数相关）| `O(n)` |
| QMADD / QMSUB | `2n` | `≈ 3·n(n-1)/2`（QFT+MADD+IQFT）| `O(n)` |
| C_QMSUB | `2n+1` | 与 QMSUB 相同，外加 1 个 CX | `O(n)` |

整条"舍入电路"各阶段调用上述模块的次数如下：
1. `C_QMSUB` ×2（求 `(a-b)`,`(c-d)`）；
2. `QMADD` ×3（`result1` 两次，Stage6 中一次）；
3. `QMSUB` ×2（`result2`、Stage6）；
4. `QMADD` ×4 + `QMSUB` ×1（恢复/全局运算）；
5. 多轮 `cswap` 与 UR 操作（UR 仅使用 SWAP + 若干 CX，深度 `O(n)`）。

因此总门数约为 `O(k·n²)`（其中 `k≈12` 为上述自定义门的调用次数），总深度为 `O(k·n)`。在 `n=4`（guard-bit 后为 5）时，电路使用 36 量子比特，可通过 `matrix_product_state` 仿真，并已由 `verify_all_inputs.py` 穷举验证全部 65,536 组输入。

### 小波系数说明

电路输出四个小波系数：

1. **`reg_d`**：低频近似子带（LL）= `min(a,b,c,d)`
   - 保留图像的主要内容和结构
   - 展示小波变换的压缩特性

2. **`result1`**：水平细节（H₁）= `⌊((a-b)+(c-d))/2⌋`
   - 捕捉横向边缘和线条

3. **`reg_a`**：垂直细节（H₂）= `⌊((a+b)-(c+d))/2⌋`
   - 捕捉纵向边缘和线条

4. **`result2`**：对角细节（H₃）= `⌊((a-b)-(c-d))/2⌋`
   - 捕捉对角边缘和角点

### 论文图表生成（基于 paper_outline_ver1.md）

#### ✅ 核心电路图（根据 paper_outline_ver1.md）

| Figure | 文件名 | 状态 | 说明 |
|--------|--------|------|------|
| **Figure 1** | - | ⚠️ 引用 | Sahin 的加法器/减法器电路结构图（需要引用文献） |
| **Figure 2** | `figure2_qmhwt_architecture.tex` | ✅ PDF已生成 | QM-HWT 总体电路架构图（7阶段流水线流程图） |
| **Figure 3** | - | ⚠️ 引用/生成 | 舍入单元 (Rounding Unit) 示意图（需要引用 QIST 文献或生成） |
| **Figure 4** | `figure4_7stage_pipeline.tex` | ✅ PDF已生成 | 7阶段流水线详细电路时序/逻辑图（分两页：Stage 1-3 和 Stage 4-7） |

**编译方式**：
```bash
# 编译 Figure 2（总体架构图）
xelatex -interaction=nonstopmode figure2_qmhwt_architecture.tex

# 编译 Figure 4（7阶段流水线）
xelatex -interaction=nonstopmode figure4_7stage_pipeline.tex
```

**注意**：Figure 1 和 Figure 3 需要引用文献，不需要生成 LaTeX 文件。

**编译方式**：

**方法1：批量编译独立文档（推荐）**
```bash
# 一次性编译 Figure 1, 4, 5, 6, 7（都是独立的完整文档）
# 方式A：逐个编译
xelatex -interaction=nonstopmode figure1_circuit_architecture.tex
xelatex -interaction=nonstopmode figure4_rqru_circuit.tex
xelatex -interaction=nonstopmode figure5_main_circuit.tex
xelatex -interaction=nonstopmode figure6_dataflow.tex
xelatex -interaction=nonstopmode figure7_guard_bit.tex

# 方式B：使用循环批量编译（推荐）
for file in figure1_circuit_architecture.tex figure4_rqru_circuit.tex figure5_main_circuit.tex figure6_dataflow.tex figure7_guard_bit.tex; do
    echo "Compiling $file..."
    xelatex -interaction=nonstopmode "$file" > /dev/null 2>&1
done
echo "All independent figures compiled!"
```

**方法2：编译 Figure 2a, 2b, 3（通过测试文档）**
```bash
# Figure 2a, 2b, 3 都在 circuit_figures_qcircuit.tex 中，需要通过测试文档编译
xelatex -interaction=nonstopmode circuit_figures_test_qcircuit.tex
# 输出：circuit_figures_test_qcircuit.pdf（包含 Figure 2a, 2b, 3）
```

**注意**：
- Figure 1, 4, 5, 6, 7 都是完整的独立 LaTeX 文档，可以单独或批量编译
- Figure 2a, 2b, 3 的源代码在 `circuit_figures_qcircuit.tex` 中，但这不是完整文档，需要通过 `circuit_figures_test_qcircuit.tex` 编译

#### ✅ 实验图表（根据 paper_outline_ver1.md）

| Figure | LaTeX文件 | 图像文件状态 | 说明 |
|--------|-----------|--------------|------|
| **Figure 5** | `figure5_house_edges.tex` | ✅ PDF已生成 | ⭐ **核心结果** - House图像边缘检测对比图（2×2布局：原图、二值化、经典Haar、QM-HWT） |
| **Figure 6** | - | ❌ 未完成 | 高频系数分布直方图（展示 QM-HWT 的系数更集中在 0 附近） |

**编译方式**：
```bash
# 编译 Figure 5（House图像边缘检测对比图）
xelatex -interaction=nonstopmode figure5_house_edges.tex
```

**生成 Figure 5 所需图像**：
```bash
# 生成二值化图像和边缘检测结果
python3 generate_figure5_binary_edges.py
# 或使用 process_house_image.py
python3 process_house_image.py --image house.tiff --threshold 128
```

**Figure 5 输出图像文件**：
- `house_original.png` - 原始House图像
- `house_binary.png` - 二值化图像（阈值=128）
- `house_binary_classical_haar_edges.png` - 经典线性Haar边缘（基于二值图）
- `house_binary_qmhwt_edges.png` - QM-HWT边缘（基于二值图）

**扩展实验（不在 paper_outline_ver1.md 中，可选）**：
- **Cameraman图像实验**：多方向特征分解（`edge_detection_experiment.py`）
  - 用于展示多方向特征分解能力
  - 不在核心论文大纲中，可作为扩展实验

#### ✅ 数据表格（根据 paper_outline_ver1.md）

| Table | LaTeX文件 | 状态 | 说明 |
|-------|-----------|------|------|
| **Table 1** | `table1_unit_test.tex` | ✅ PDF已生成 | 单元测试输入输出对照表（理论值 vs 量子电路模拟值） |
| **Table 2** | - | ❌ 未完成 | 稀疏性指标统计表（零值比例、熵值等具体数据） |
| **Table 3** | - | ❌ 未完成 | 复杂度对比表（经典算法、线性量子小波与本文方法的空间/时间复杂度） |

**生成/更新脚本**：
```bash
# 生成 Table 1（单元测试对照表）
python3 generate_table1_unit_test.py

# 编译 Table 1
xelatex -interaction=nonstopmode table1_unit_test.tex
```

### 图表生成工具脚本（根据 paper_outline_ver1.md）

#### ✅ 核心图表生成（已完成）

- `generate_table1_unit_test.py` - 生成 Table 1 的 LaTeX 代码（单元测试输入输出对照表）⭐ **核心**
- `generate_figure5_binary_edges.py` - 生成 Figure 5 所需图像（House图像边缘检测对比）⭐ **核心**
- `process_house_image.py` - 处理 House 图像并生成边缘检测结果 ⭐ **核心应用**

#### ❌ 待完成的图表生成脚本

- `generate_figure6_histogram.py` - 生成 Figure 6（高频系数分布直方图）❌ **未完成**
- `generate_table2_sparsity.py` - 生成 Table 2（稀疏性指标统计表）❌ **未完成**
- `generate_table3_complexity.py` - 生成 Table 3（复杂度对比表）❌ **未完成**

#### 🔬 扩展实验脚本（不在 paper_outline_ver1.md 中，可选）

以下脚本不在核心论文大纲中，可作为扩展实验使用：

- `edge_detection_experiment.py` - Cameraman图像多方向特征分解实验
- `generate_figure12_histogram.py` - 系数分布直方图（扩展实验）
- `generate_tables3_4.py` - 稀疏性和SSIM表格（扩展实验）
- `multi_image_experiment.py` - 多图像测试实验（扩展实验）
- `parameter_comparison_experiment.py` - 参数对比实验（扩展实验）

### 相关文档

- `paper_outline_ver1.md`：⭐ **最新论文大纲** - 基于此版本确定核心图表和实验
- `ANALYSIS_PAPER_OUTLINE.md`：基于 paper_outline_ver1.md 的电路和实验保留分析
- `circuit_analysis.md`：电路特性分析
- `THRESHOLD_FUSION_EXPLANATION.md`：阈值化融合方法详细说明

### 📋 核心内容总结（根据 paper_outline_ver1.md）

**应该保留的核心内容**：
1. ✅ **电路实现**：`circuit/` 目录下的所有核心电路文件
2. ✅ **验证脚本**：`verify_all_inputs.py`, `test_rounding.py`, `complexity_analysis.py`
3. ✅ **House图像实验**：`process_house_image.py`, `generate_figure5_binary_edges.py` ⭐ **核心应用**
4. ✅ **核心图表**：Figure 2, 4, 5, Table 1

**可选/扩展内容**：
- Cameraman图像实验（`edge_detection_experiment.py`）
- 多图像实验（不在核心大纲中）
- 参数对比实验（不在核心大纲中）

**待完成的内容**：
- Figure 6：高频系数分布直方图
- Table 2：稀疏性指标统计表
- Table 3：复杂度对比表

